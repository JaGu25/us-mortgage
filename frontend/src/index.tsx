import React from 'react';
import ReactDOM from 'react-dom';
import AppAdmin from './AppMortgage';

ReactDOM.render(
  <React.StrictMode>
    <AppAdmin />
  </React.StrictMode>,
  document.getElementById('root')

);
